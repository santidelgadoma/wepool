# Progreso — Carpool ITAM MVP

Meta: llegar a una **demo funcional para inversionistas**. Este archivo se actualiza conforme avanza el desarrollo.

Última actualización: 17 de agosto de 2026

## Modelo de negocio / pitch (para tener en cuenta en decisiones de producto)

- **Es un producto institucional, no solo para el ITAM.** El ITAM es el primer cliente/piloto, pero el pitch de comercialización es vender esto como servicio a cualquier empresa o institución que quiera conectar a su gente por este medio de transporte — el ITAM es un ejemplo, no un límite del producto.
- **Modelo de monetización: micro-earning.** El conductor gana dinero por viajes que de todos modos iba a hacer (ir al trabajo/universidad); el pasajero paga un precio bajo. El diferenciador competitivo frente a Uber/Didi es precisamente que el precio puede ser mucho menor, porque no es el ingreso principal del conductor — es una ganancia marginal por un trayecto que ya iba a hacer.
- **2026-08-17: ambos gaps ya se cerraron en el código** (ver Fase 2 y Fase 3 abajo, y la nota extendida al final de este archivo): el dominio institucional ya no está hardcodeado a `@itam.mx` (tabla `institutions`, `supabase/migrations/0004_instituciones.sql`), y `/consultar`, `/manana` y `/historial` ya muestran una estimación de precio/ganancia (`lib/pricing.ts`). Sigue pendiente aplicar la migración `0004` en el proyecto real de Supabase — ver la nota al final.

## Fase 0 — Fundaciones (arquitectura)

- [x] Revisión de la tesina y tecnologías usadas en el prototipo académico
- [x] Definición de arquitectura del MVP: Next.js + TypeScript + Supabase (Postgres + PostGIS + Auth)
- [x] Diseño detallado del esquema de base de datos (tablas + políticas RLS) — ver `docs/esquema_base_datos.md` y `supabase/migrations/`
- [ ] Wireframes de las pantallas clave

## Fase 1 — Infraestructura base

- [x] Proyecto Next.js 15 + TypeScript + Tailwind creado (código en la carpeta del proyecto)
- [x] `git init` + primer commit — repo en `github.com/santidelgadoma/wepool`
- [x] Proyecto de Supabase creado (dev)
- [x] Proyecto de Vercel conectado al repo (`wepool-op34`, deploy de producción exitoso)
- [x] Migraciones `0001_init_schema.sql` y `0002_functions.sql` aplicadas en el proyecto real
- [x] Auth Hook de dominio institucional conectado desde el panel de Supabase (Authentication → Hooks)
- [x] Extensión PostGIS habilitada (parte de la migración ya aplicada)
- [x] `npm install` + `npm run build` corridos (localmente por el usuario, y en Vercel) — ver nota del fix de TypeScript abajo

## Fase 2 — Autenticación

- [x] Registro con correo institucional — código escrito en `app/(auth)/registro`
- [x] Verificación de correo — **cambiada a link mágico** (ver nota abajo); `app/auth/callback/route.ts` intercambia el código por sesión. Falta agregar `http://localhost:3000/auth/callback` a Redirect URLs en el panel de Supabase.
- [x] Login / sesión — `app/(auth)/login` + middleware de protección de rutas
- [x] Políticas de Row Level Security por tabla — ya en `0001_init_schema.sql`, aplicadas
- [x] Registro **multi-institución** — el dominio de correo permitido ya no está hardcodeado a `@itam.mx`; se valida contra una tabla `institutions` (`supabase/migrations/0004_instituciones.sql`, pendiente de aplicar en el proyecto real). El ITAM queda como la primera institución/piloto.

## Fase 3 — Funcionalidad core

- [x] Publicar viaje como conductor (ida) — pantalla `/reserva`, un solo formulario cubre conductor/pasajero × ida/regreso (ver nota abajo)
- [x] Publicar viaje como conductor (regreso)
- [x] Reservar viaje como pasajero (ida)
- [x] Reservar viaje como pasajero (regreso)
- [x] Algoritmo de emparejamiento con PostGIS (+ estimación temporal de duración; Google Distance Matrix queda para Fase 4) — pantalla `/consultar`, ver nota abajo
- [x] Confirmación de viaje (conductor elige pasajero / pasajero elige viaje) — `lib/actions/consultar.ts` → `elegirCandidato`
- [x] Cancelación de reservación — pantalla `/cancelar`
- [x] Historial de viajes — pantalla `/historial`
- [x] Vista "viajes de mañana" — pantalla `/manana`
- [x] Estimación de precio (pasajero) / ganancia (conductor) — `lib/pricing.ts`, mostrada en `/consultar`, `/manana` e `/historial`. Sin cobro real todavía (pendiente Stripe u otro procesador, Fase 4/5+).

## Fase 4 — Integraciones externas

- [~] Geocoding — implementado con **OpenStreetMap Nominatim** (gratuito, sin API key) como solución temporal, ver nota abajo. Falta reemplazar por Google Geocoding cuando se conecte `GOOGLE_MAPS_API_KEY`.
- [ ] Distance Matrix (Google) — para el paso 2 del emparejamiento (confirmar desvío real del conductor), ver `docs/esquema_base_datos.md` sección 3
- [ ] Creación de evento en Google Calendar al confirmar viaje
- [ ] Correos de recordatorio (Resend)
- [ ] Limpieza automática de reservaciones vencidas (pg_cron) — la función y el `cron.schedule` ya están en `0002_functions.sql`; falta solo verificar que corrió en el proyecto real

## Fase 5 — Pulido para demo

- [ ] Diseño visual con shadcn/ui aplicado a las 8 pantallas
- [ ] Datos de ejemplo (seed) realistas para la demo
- [x] Prueba de flujo completo extremo a extremo — suite de Playwright (`e2e/demo-flow.spec.ts`) corrida con éxito de principio a fin: publicar viaje (conductor y pasajero), emparejamiento, confirmación de ambos lados, y aparición en "mañana" para los dos (ver nota abajo)
- [x] Dominio público desplegado (Vercel)
- [ ] Guion / narrativa de la demo para inversionistas

## Notas y decisiones

- 2026-08-14: Se decidió arquitectura "todo en TypeScript" (Next.js + Supabase) en vez de mantener un backend Python separado, priorizando velocidad de desarrollo hacia la demo. Ver `docs/arquitectura_mvp.md`.
- 2026-08-14: Se diseñó el esquema de base de datos consolidando las 4 tablas de reservación de la tesina en una sola (`trip_offers`), con emparejamiento geoespacial vía PostGIS (`find_candidate_offers`) y restricción de registro a correo institucional vía Supabase Auth Hook. Ver `docs/esquema_base_datos.md`.
- 2026-08-14 (noche): Proyecto de Supabase real creado, ambas migraciones aplicadas y Auth Hook de dominio institucional conectado.
- 2026-08-14 (noche): Se armó el proyecto Next.js (App Router, TypeScript, Tailwind, componentes estilo shadcn/ui escritos a mano, clientes de Supabase para browser/server/middleware, las 8 pantallas de la tesina enrutadas). Registro, verificación, login/logout y `/home` implementados de verdad; reserva/cancelar/consultar/historial/mañana eran placeholders para Fase 3 (ya no).
- 2026-08-14 (noche): Se cambió el registro a **link mágico** en vez de código de 6 dígitos, por la restricción de Supabase (desde el 3 de junio de 2026) de personalizar plantillas de correo en plan gratuito sin SMTP propio. Documentado en `docs/arquitectura_mvp.md` y `docs/esquema_base_datos.md`.
- 2026-08-17: `git init` + primer commit hechos por el usuario (repo `santidelgadoma/wepool` en GitHub). Al conectar Vercel, el primer deploy falló con `Type error: Parameter 'cookiesToSet' implicitly has an 'any' type` en `lib/supabase/middleware.ts` y `server.ts` — el patrón de `@supabase/ssr` para `setAll(cookiesToSet)` pierde el tipado contextual cuando el objeto de cookies se resuelve contra un tipo unión (`CookieMethodsServer`), así que TypeScript en modo estricto lo marca como `any` implícito. Se corrigió anotando explícitamente `cookiesToSet: { name: string; value: string; options?: CookieOptions }[]` (con `CookieOptions` importado de `@supabase/ssr`, confirmado que existe revisando el código fuente del paquete). Deploy de producción exitoso después del fix: `wepool-op34-*.vercel.app`.
- 2026-08-17: Se implementó Fase 3 (publicar/reservar viaje, cancelar, historial, mañana) contra el esquema real (`trip_offers`, `confirmed_trips`) usando Server Actions de Next.js + RLS (sin necesitar la llave de servicio de Supabase). Decisiones:
  - **Geocoding temporal con OpenStreetMap Nominatim** (`lib/geocoding.ts`) en vez de esperar a `GOOGLE_MAPS_API_KEY` — decisión del usuario para no bloquear Fase 3. Toda la app pasa por esta única función, así que cambiar a Google Geocoding en Fase 4 es reescribir un archivo, no tocar el resto del código.
  - Un solo formulario en `/reserva` (`components/reserva-form.tsx`) cubre los 4 casos de PROGRESS.md (conductor/pasajero × ida/regreso), tal como ya sugería el placeholder original y el diseño de `trip_offers` (una tabla con columnas `direction`/`role` en vez de 4 tablas).
  - `lib/datetime.ts` centraliza la regla "solo para mañana" y las conversiones de zona horaria, asumiendo offset fijo UTC-6 para America/Mexico_City (México ya no observa horario de verano desde 2022).
  - **Pendiente y bloqueado:** "Consultar viajes disponibles" (`/consultar`, ejecuta `find_candidate_offers`) y la confirmación de viaje requieren `SUPABASE_SERVICE_ROLE_KEY` (Settings → API → service_role en el panel de Supabase) porque esa función está restringida a `service_role` a propósito (ver `docs/esquema_base_datos.md` sección 3, nota de seguridad). No se ha pedido/agregado esa llave todavía — es el siguiente pendiente real de Fase 3.
- 2026-08-17: El usuario compartió la `SUPABASE_SERVICE_ROLE_KEY` y se completó Fase 3: `/consultar` (emparejamiento) y la confirmación de viaje. Decisiones/hallazgos:
  - PostgREST no serializa columnas `geography` como JSON legible en respuestas normales (llegan como WKB hexadecimal — confirmado revisando la guía oficial de PostGIS de Supabase), así que en vez de decodificar eso en JS se agregó `supabase/migrations/0003_matching_helpers.sql` con una función `distance_between_offers(uuid, uuid)` que calcula la distancia en metros directamente en Postgres (restringida a `service_role`, mismo patrón que `find_candidate_offers`). **Esta migración todavía no se ha aplicado al proyecto real** — pendiente que el usuario la corra (SQL Editor del panel de Supabase, o `supabase db push`).
  - `lib/supabase/admin.ts`: cliente con la llave de servicio, usado SOLO desde Server Actions (`lib/actions/consultar.ts`), nunca desde componentes de cliente. Cada función que lo usa valida a mano (contra `supabase.auth.getUser()` del cliente normal, no el admin) que el usuario autenticado es dueño de la oferta antes de dejarlo actuar — la llave de servicio salta RLS, así que esa validación manual es la única barrera.
  - La duración mostrada en `/consultar` es una estimación (distancia en línea recta ÷ velocidad promedio de 22 km/h) hasta que se conecte Google Distance Matrix en Fase 4 — está señalado en la UI, no se presenta como dato real.
  - `SUPABASE_SERVICE_ROLE_KEY` se agregó a `.env.local` y a las variables de entorno de Vercel (Production). **Nunca** lleva prefijo `NEXT_PUBLIC_`.
- 2026-08-17: Se armó la suite de tests end-to-end con **Playwright**, cubriendo el flujo completo de la demo: dos usuarios de prueba (`e2e.conductor@itam.mx` y `e2e.pasajero@itam.mx`) publican viajes compatibles en `/reserva`, se emparejan en `/consultar`, ambos confirman (pasajero primero, luego conductor) y el viaje confirmado aparece en `/manana` para los dos. Decisiones:
  - Los usuarios de prueba se crean/reutilizan automáticamente antes de cada corrida (`e2e/global-setup.ts`), usando la API de administración de Supabase Auth (`auth.admin.createUser` / `listUsers`) con `SUPABASE_SERVICE_ROLE_KEY` — así se saltan el flujo de correo real y no hace falta una bandeja de entrada. El mismo setup limpia `confirmed_trips`/`trip_offers`/`vehicles` de ambos usuarios antes de correr, para que la suite sea repetible.
  - `playwright.config.ts` puede correr contra `npm run dev` local (se levanta solo) o contra el deploy real de Vercel vía `PLAYWRIGHT_BASE_URL=https://<dominio> npm run test:e2e` — útil para probar la demo tal como la va a ver un inversionista, no solo en local.
  - Fix de UX que salió de escribir el test: `crearOferta` (`lib/actions/reserva.ts`) ahora regresa `success: true` al publicar, y `reserva-form.tsx` muestra un banner de confirmación ("¡Viaje publicado!") — antes el formulario no daba ninguna señal visual de éxito, lo cual además es la señal que usa el test para saber que el submit funcionó.
  - **Pendiente:** correr la suite por primera vez requiere `npm install` (para traer `@playwright/test`, ya en `package.json`) — no se pudo correr en este entorno de trabajo por no tener acceso al registro de npm, así que queda como siguiente paso del usuario. Instrucciones completas en la sección "Tests" de `README.md`.
- 2026-08-17: Al intentar `npx playwright install chromium` en la Mac del usuario (macOS 13/Ventura) falló con `Playwright does not support chromium on mac13` — desde hace tiempo Playwright dejó de publicar binarios de Chromium propios para macOS anterior a Sonoma (14). Se cambió `playwright.config.ts` para usar `channel: "chrome"` en vez del Chromium descargado por Playwright: los tests corren sobre el Google Chrome que ya está instalado en la máquina (necesario de todos modos para Claude en Chrome), así que no hace falta descargar ningún navegador ni depender de la versión de macOS. Confirmado por búsqueda en la documentación oficial de Playwright que `channel: "chrome"` evita el paso de `playwright install` por completo.
- 2026-08-17: Primera corrida real de `npm run test:e2e` falló en `global-setup.ts` con `Node.js detected but native WebSocket not found` al crear el cliente de Supabase con la llave de servicio. Causa: una versión reciente de `@supabase/supabase-js` (el rango `^2.45.0` del `package.json` resuelve a la última 2.x al correr `npm install`) exige `WebSocket` nativo del lado de Node, disponible solo desde Node 22 — el Node local del usuario es v20 (ver el warning de deprecación de Supabase en la misma corrida). En vez de pedirle que actualice Node a nivel de sistema (rompería otras herramientas y no protege a quien clone el repo con otra versión), se agregó la librería `ws` como dependencia y se le pasa explícitamente como `realtime.transport` en **ambos** lugares donde se crea un cliente con llave de servicio: `lib/supabase/admin.ts` (usado en producción por `/consultar`) y `e2e/global-setup.ts`. Con esto el proyecto funciona igual sin importar la versión de Node — se investigó y confirmó el fix exacto en la documentación pública de Supabase (`github.com/orgs/supabase/discussions/37869`). Como se agregó una dependencia nueva, hace falta correr `npm install` de nuevo antes de volver a probar.
- 2026-08-17: El test E2E encontró un bug real: al confirmar el viaje como conductor (segundo paso del emparejamiento), la base de datos sí se actualizaba pero la pantalla `/consultar` se quedaba con el botón "Elegir este viaje" visible, como si nada hubiera pasado. Causa: `revalidatePath("/consultar")` dentro de `elegirCandidato` invalida la caché del **servidor**, pero no obliga al **cliente** a re-pedir la ruta cuando la Server Action se llama fuera de un `<form action={...}>` (aquí se llama directo desde `onClick` dentro de `startTransition`, en `elegir-boton.tsx` y `cancelar-boton.tsx`). El paso del pasajero en el test "funcionaba" solo porque justo después hay una navegación completa (`page.goto`) que sí trae datos frescos — el bug estaba escondido porque ningún flujo anterior se había probado sin una navegación de por medio. Fix: se agregó `router.refresh()` (de `next/navigation`) después de una mutación exitosa en `elegir-boton.tsx` y, por el mismo patrón, también en `cancelar-boton.tsx` aunque el test no lo cubre directamente. Este bug ya afectaba producción (no solo el test) — cualquiera que confirmara un viaje como conductor sin recargar la página manualmente veía la pantalla desactualizada.
- 2026-08-17: El mismo test volvió a fallar exactamente igual después del fix de `router.refresh()`, sin ningún mensaje de error en pantalla — señal de que la falla real quedaba oculta en vez de resuelta. Se encontraron dos huecos: (1) `elegirCandidato`/`cancelarOferta` no tenían `try/catch`, así que cualquier excepción real (no un `.error` controlado de Supabase) rechazaba la promesa dentro del `startTransition` y React se lo tragaba en silencio, sin mostrar nada ni loguear nada; (2) `router.refresh()` se llamaba directo después de un `await` dentro del mismo `startTransition` async, que no es el patrón que documenta Next.js — la recomendación es envolverlo en su propio `startTransition` separado. Se corrigieron ambas cosas en `elegir-boton.tsx`/`cancelar-boton.tsx` (try/catch + `console.error` + refresh en su propio transition) y en `lib/actions/consultar.ts`/`lib/actions/cancelar.ts` (la lógica se movió a una función interna envuelta en try/catch en la exportada, logueando cualquier excepción con `console.error` — visible en la terminal de `npm run dev` y en el output `[WebServer]` que captura Playwright). Si el test vuelve a fallar, la terminal ahora debería mostrar el motivo real en vez de un fallo mudo.
- 2026-08-17: Tercera corrida — el test volvió a fallar en el mismo punto, esta vez sin ninguna excepción en la terminal. Se leyó `error-context.md` directo de la carpeta `test-results/` en la Mac del usuario (vía el puente de archivos) para ver el estado exacto en el momento de la falla: la duración estimada del candidato había cambiado entre corridas (~7 min → ~6 min), lo que confirmó que `router.refresh()` sí estaba funcionando y recalculando todo desde cero — pero seguía encontrando la misma oferta del conductor activa. **Bug real encontrado:** en `elegirCandidato`, el `delete` de `trip_offers` después de confirmar el viaje nunca revisaba su propio error. Si esas ofertas todavía estaban referenciadas por una fila de `trip_matches` (no se puede confirmar que el esquema real tenga `ON DELETE CASCADE` ahí, a pesar del comentario que lo asumía), el delete fallaba por violación de llave foránea — en silencio, porque el error nunca se leía — y la función igual regresaba `{success: true}` habiendo insertado `confirmed_trips` pero sin haber borrado las ofertas. Fix: ahora se borran a mano las filas de `trip_matches` que referencian esas ofertas *antes* de borrar las ofertas, y si el delete de ofertas falla de todos modos, se revisa el error y se regresa un mensaje en vez de tragárselo. Se aplicó la misma limpieza preventiva en `cancelarOferta` (ahora usa `lib/supabase/admin.ts` para ese paso) — un usuario cancelando una reservación que ya tenía un candidato calculado se hubiera topado con el mismo choque de llave foránea durante la demo real, aunque ahí sí se veía el error (confuso) en vez de fallar en silencio.
- 2026-08-17: Cuarta corrida — con el logging del fix anterior, el error real por fin apareció en la terminal: `violates foreign key constraint "confirmed_trips_match_id_fkey" on table "confirmed_trips"`. Diagnóstico definitivo revisando `supabase/migrations/0001_init_schema.sql`: `trip_offers -> trip_matches` sí tiene `on delete cascade` (el comentario original tenía razón en eso), pero `confirmed_trips.match_id -> trip_matches.id` **no** tiene cascade — a propósito, para conservar el vínculo histórico con el match que originó cada viaje confirmado. Eso significa que el `trip_matches` recién usado para crear un `confirmed_trips` es, por diseño del esquema, imposible de borrar (ni directo ni por cascada al borrar `trip_offers`) — el intento de "limpiar" agregado en la corrida anterior estaba resolviendo el síntoma equivocado. **Fix correcto:** en vez de borrar las ofertas, se actualiza su `status` a `'confirmado'` — valor que el enum `trip_offer_status` ya incluía (`buscando`/`confirmado`/`cancelado`/`expirado`) pero que nunca se había usado. `obtenerCandidatos()` ya filtra por `status = 'buscando'` en todos lados, así que una oferta confirmada deja de aparecer como candidato sin tocar `trip_matches` para nada. Se verificó que `/cancelar` (filtra por `status = 'buscando'`) y `/historial` (lee de `confirmed_trips`, no de `trip_offers`) no se ven afectados por dejar de borrar las filas de `trip_offers`.
- 2026-08-17: **Quinta corrida — la suite completa pasó de principio a fin.** El flujo que va a ver un inversionista (conductor y pasajero publican, se emparejan, ambos confirman, el viaje aparece en "mañana" para los dos) queda validado automáticamente contra el proyecto real de Supabase. En el camino se encontraron y corrigieron tres bugs reales que no se habían detectado a mano: (1) el paso 2 de la confirmación no refrescaba la pantalla sin recargar manualmente (`router.refresh()` faltante), (2) los errores de las Server Actions podían quedar completamente mudos si algo tronaba de forma inesperada, y (3) el `delete` de ofertas al confirmar un viaje violaba una llave foránea contra `confirmed_trips` y fallaba en silencio — el más serio de los tres, porque un viaje "confirmado" se hubiera quedado mostrándose como disponible para siempre. Este es el mejor argumento a favor de haber escrito la suite antes de la demo: los tres bugs eran invisibles probando a mano una sola vez, pero rompían el flujo real de un inversionista viendo el emparejamiento en detalle.
- 2026-08-17: El usuario aclaró el pitch real del producto (institucional, no exclusivo del ITAM; modelo de micro-earning conductor/pasajero) y eligió cerrar ambos gaps en el código antes de seguir con seed data o pulido visual — ver decisiones tomadas vía `AskUserQuestion`. Cambios:
  - **Multi-institución:** `supabase/migrations/0004_instituciones.sql` agrega la tabla `institutions` (nombre + dominio de correo), migra `profiles.institution_id` (`not null`, con el ITAM como valor de todos los perfiles existentes), reescribe `handle_new_user` para resolver la institución por dominio al crear el perfil, y reescribe `restrict_signup_to_itam_domain` para validar contra la tabla en vez de un dominio hardcodeado — **se mantiene el mismo nombre de función a propósito**, para no tener que reconectar el hook "Before User Created" en el panel de Supabase. `find_candidate_offers` ahora también exige `institution_id` igual entre ambas ofertas — sin esto, dos empresas cliente distintas se emparejarían entre sí. Se actualizó la copia de `/registro`, `/login` y `/home` para dejar de mencionar `@itam.mx` explícitamente. **Pendiente crítico: esta migración (`0004`) todavía no se ha aplicado al proyecto real de Supabase** — sin aplicarla, el registro seguiría funcionando con el Auth Hook viejo (que solo acepta `@itam.mx`) porque la función en la base de datos real todavía no se sobrescribió. Aplicar igual que las migraciones anteriores (SQL Editor o `supabase db push`), ver `docs/esquema_base_datos.md` sección 8.
  - **Precio/ganancia estimada:** `lib/pricing.ts` calcula un precio al pasajero y una ganancia al conductor a partir de la misma distancia en línea recta que ya se usa para la duración estimada — sin tabla ni columna nueva, sin cobro real todavía. Se muestra en `/consultar` ("Vas a pagar/ganar ~$X"), `/manana` y `/historial` (estos dos reconstruyen la distancia desde `trip_matches.estimated_duration_minutes`, embebido vía `confirmed_trips.match_id` — que sigue existiendo gracias al fix de la corrida anterior). Fórmula: tarifa base $10 MXN + $3.5 MXN/km, con 15% de comisión de plataforma sobre lo que paga el pasajero.
  - Se actualizó `docs/esquema_base_datos.md` (secciones 4, 5 y nueva sección 9) para reflejar ambos cambios y corregir la sección 5, que todavía describía el diseño viejo de "borrar" ofertas confirmadas.
  - **No se tocó:** seed data con 2+ instituciones para demostrar visualmente la separación multi-tenant en la demo — es el siguiente paso natural ahora que la tabla `institutions` ya existe, pendiente de que el usuario decida seguir con eso.
