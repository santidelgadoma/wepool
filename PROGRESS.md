# Progreso — WEPOOL MVP

> El proyecto se llamaba "Carpool ITAM" durante el desarrollo inicial; el 2026-08-18 se definió el nombre de marca **WEPOOL** (logo + paleta de colores propios). Las notas de abajo con fecha anterior a esa se dejan tal cual se escribieron, mencionan "Carpool ITAM" porque así se llamaba el proyecto en ese momento — no se reescribe el historial.

Meta: llegar a una **demo funcional para inversionistas**. Este archivo se actualiza conforme avanza el desarrollo.

Última actualización: 18 de agosto de 2026

## Modelo de negocio / pitch (para tener en cuenta en decisiones de producto)

- **Es un producto institucional, no solo para el ITAM.** El ITAM es el primer cliente/piloto, pero el pitch de comercialización es vender esto como servicio a cualquier empresa o institución que quiera conectar a su gente por este medio de transporte — el ITAM es un ejemplo, no un límite del producto.
- **Modelo de monetización: micro-earning.** El conductor gana dinero por viajes que de todos modos iba a hacer (ir al trabajo/universidad); el pasajero paga un precio bajo. El diferenciador competitivo frente a Uber/Didi es precisamente que el precio puede ser mucho menor, porque no es el ingreso principal del conductor — es una ganancia marginal por un trayecto que ya iba a hacer.
- **2026-08-17: ambos gaps ya se cerraron en el código** (ver Fase 2 y Fase 3 abajo, y la nota extendida al final de este archivo): el dominio institucional ya no está hardcodeado a `@itam.mx` (tabla `institutions`, `supabase/migrations/0004_instituciones.sql`), y `/consultar`, `/manana` y `/historial` ya muestran una estimación de precio/ganancia (`lib/pricing.ts`). Sigue pendiente aplicar la migración `0004` en el proyecto real de Supabase — ver la nota al final.

## Fase 0 — Fundaciones (arquitectura)

- [x] Revisión de la tesina y tecnologías usadas en el prototipo académico
- [x] Definición de arquitectura del MVP: Next.js + TypeScript + Supabase (Postgres + PostGIS + Auth)
- [x] Diseño detallado del esquema de base de datos (tablas + políticas RLS) — ver `docs/esquema_base_datos.md` y `supabase/migrations/`
- [ ] Wireframes de las pantallas clave

## Fase 1 — Infraestructura base

- [x] Proyecto Next.js 15 + TypeScript + Tailwind creado (código en la carpeta del proyecto)
- [x] `git init` + primer commit — repo en `github.com/santidelgadoma/wepool`
- [x] Proyecto de Supabase creado (dev)
- [x] Proyecto de Vercel conectado al repo (`wepool-op34`, deploy de producción exitoso)
- [x] Migraciones `0001_init_schema.sql` y `0002_functions.sql` aplicadas en el proyecto real
- [x] Auth Hook de dominio institucional conectado desde el panel de Supabase (Authentication → Hooks)
- [x] Extensión PostGIS habilitada (parte de la migración ya aplicada)
- [x] `npm install` + `npm run build` corridos (localmente por el usuario, y en Vercel) — ver nota del fix de TypeScript abajo

## Fase 2 — Autenticación

- [x] Registro con correo institucional — código escrito en `app/(auth)/registro`
- [x] Verificación de correo — **cambiada a link mágico** (ver nota abajo); `app/auth/callback/route.ts` intercambia el código por sesión. Falta agregar `http://localhost:3000/auth/callback` a Redirect URLs en el panel de Supabase.
- [x] Login / sesión — `app/(auth)/login` + middleware de protección de rutas
- [x] Políticas de Row Level Security por tabla — ya en `0001_init_schema.sql`, aplicadas
- [x] Registro **multi-institución** — el dominio de correo permitido ya no está hardcodeado a `@itam.mx`; se valida contra una tabla `institutions` (`supabase/migrations/0004_instituciones.sql`, pendiente de aplicar en el proyecto real). El ITAM queda como la primera institución/piloto.

## Fase 3 — Funcionalidad core

- [x] Publicar viaje como conductor (ida) — pantalla `/reserva`, un solo formulario cubre conductor/pasajero × ida/regreso (ver nota abajo)
- [x] Publicar viaje como conductor (regreso)
- [x] Reservar viaje como pasajero (ida)
- [x] Reservar viaje como pasajero (regreso)
- [x] Algoritmo de emparejamiento con PostGIS (+ estimación temporal de duración; Google Distance Matrix queda para Fase 4) — pantalla `/consultar`, ver nota abajo
- [x] Confirmación de viaje (conductor elige pasajero / pasajero elige viaje) — `lib/actions/consultar.ts` → `elegirCandidato`
- [x] Cancelación de reservación — pantalla `/cancelar`
- [x] Historial de viajes — pantalla `/historial`
- [x] Vista "viajes de mañana" — pantalla `/manana`
- [x] Estimación de precio (pasajero) / ganancia (conductor) — `lib/pricing.ts`, mostrada en `/consultar`, `/manana` e `/historial`. Sin cobro real todavía (pendiente Stripe u otro procesador, Fase 4/5+).

## Fase 4 — Integraciones externas

- [~] Geocoding — implementado con **OpenStreetMap Nominatim** (gratuito, sin API key) como solución temporal, ver nota abajo. Falta reemplazar por Google Geocoding cuando se conecte `GOOGLE_MAPS_API_KEY`.
- [ ] Distance Matrix (Google) — para el paso 2 del emparejamiento (confirmar desvío real del conductor), ver `docs/esquema_base_datos.md` sección 3
- [ ] Creación de evento en Google Calendar al confirmar viaje
- [ ] Correos de recordatorio (Resend)
- [ ] Limpieza automática de reservaciones vencidas (pg_cron) — la función y el `cron.schedule` ya están en `0002_functions.sql`; falta solo verificar que corrió en el proyecto real

## Fase 5 — Pulido para demo

- [x] Diseño visual con shadcn/ui aplicado a las 8 pantallas — nav con estado activo, badges de color, iconos (lucide-react) y dashboard de accesos rápidos en `/home` (ver nota abajo)
- [x] Datos de ejemplo (seed) realistas para la demo — `scripts/seed.ts` (`npm run seed`) ya corrido al menos una vez (confirmado indirectamente: los usuarios de seed aparecieron como candidatos reales durante una corrida de E2E, ver nota abajo). 8 usuarios @itam.mx de zonas reales de CDMX, 2 parejas confirmadas + 2 sin emparejar.
- [x] Prueba de flujo completo extremo a extremo — suite de Playwright (`e2e/demo-flow.spec.ts`) corrida con éxito de principio a fin: publicar viaje (conductor y pasajero), emparejamiento, confirmación de ambos lados, y aparición en "mañana" para los dos (ver nota abajo)
- [x] Dominio público desplegado (Vercel)
- [x] Identidad de marca (WEPOOL) — logo real + paleta de colores propia aplicada en toda la app (ver nota abajo)
- [x] Guion / narrativa de la demo para inversionistas — `docs/guion_demo.md` (ver nota abajo)
- [x] Instalable como app (PWA) — manifest, iconos e ícono en pantalla de inicio, sin app nativa (ver nota abajo y `docs/producto.md` sección 12)

## Notas y decisiones

- 2026-08-14: Se decidió arquitectura "todo en TypeScript" (Next.js + Supabase) en vez de mantener un backend Python separado, priorizando velocidad de desarrollo hacia la demo. Ver `docs/arquitectura_mvp.md`.
- 2026-08-14: Se diseñó el esquema de base de datos consolidando las 4 tablas de reservación de la tesina en una sola (`trip_offers`), con emparejamiento geoespacial vía PostGIS (`find_candidate_offers`) y restricción de registro a correo institucional vía Supabase Auth Hook. Ver `docs/esquema_base_datos.md`.
- 2026-08-14 (noche): Proyecto de Supabase real creado, ambas migraciones aplicadas y Auth Hook de dominio institucional conectado.
- 2026-08-14 (noche): Se armó el proyecto Next.js (App Router, TypeScript, Tailwind, componentes estilo shadcn/ui escritos a mano, clientes de Supabase para browser/server/middleware, las 8 pantallas de la tesina enrutadas). Registro, verificación, login/logout y `/home` implementados de verdad; reserva/cancelar/consultar/historial/mañana eran placeholders para Fase 3 (ya no).
- 2026-08-14 (noche): Se cambió el registro a **link mágico** en vez de código de 6 dígitos, por la restricción de Supabase (desde el 3 de junio de 2026) de personalizar plantillas de correo en plan gratuito sin SMTP propio. Documentado en `docs/arquitectura_mvp.md` y `docs/esquema_base_datos.md`.
- 2026-08-17: `git init` + primer commit hechos por el usuario (repo `santidelgadoma/wepool` en GitHub). Al conectar Vercel, el primer deploy falló con `Type error: Parameter 'cookiesToSet' implicitly has an 'any' type` en `lib/supabase/middleware.ts` y `server.ts` — el patrón de `@supabase/ssr` para `setAll(cookiesToSet)` pierde el tipado contextual cuando el objeto de cookies se resuelve contra un tipo unión (`CookieMethodsServer`), así que TypeScript en modo estricto lo marca como `any` implícito. Se corrigió anotando explícitamente `cookiesToSet: { name: string; value: string; options?: CookieOptions }[]` (con `CookieOptions` importado de `@supabase/ssr`, confirmado que existe revisando el código fuente del paquete). Deploy de producción exitoso después del fix: `wepool-op34-*.vercel.app`.
- 2026-08-17: Se implementó Fase 3 (publicar/reservar viaje, cancelar, historial, mañana) contra el esquema real (`trip_offers`, `confirmed_trips`) usando Server Actions de Next.js + RLS (sin necesitar la llave de servicio de Supabase). Decisiones:
  - **Geocoding temporal con OpenStreetMap Nominatim** (`lib/geocoding.ts`) en vez de esperar a `GOOGLE_MAPS_API_KEY` — decisión del usuario para no bloquear Fase 3. Toda la app pasa por esta única función, así que cambiar a Google Geocoding en Fase 4 es reescribir un archivo, no tocar el resto del código.
  - Un solo formulario en `/reserva` (`components/reserva-form.tsx`) cubre los 4 casos de PROGRESS.md (conductor/pasajero × ida/regreso), tal como ya sugería el placeholder original y el diseño de `trip_offers` (una tabla con columnas `direction`/`role` en vez de 4 tablas).
  - `lib/datetime.ts` centraliza la regla "solo para mañana" y las conversiones de zona horaria, asumiendo offset fijo UTC-6 para America/Mexico_City (México ya no observa horario de verano desde 2022).
  - **Pendiente y bloqueado:** "Consultar viajes disponibles" (`/consultar`, ejecuta `find_candidate_offers`) y la confirmación de viaje requieren `SUPABASE_SERVICE_ROLE_KEY` (Settings → API → service_role en el panel de Supabase) porque esa función está restringida a `service_role` a propósito (ver `docs/esquema_base_datos.md` sección 3, nota de seguridad). No se ha pedido/agregado esa llave todavía — es el siguiente pendiente real de Fase 3.
- 2026-08-17: El usuario compartió la `SUPABASE_SERVICE_ROLE_KEY` y se completó Fase 3: `/consultar` (emparejamiento) y la confirmación de viaje. Decisiones/hallazgos:
  - PostgREST no serializa columnas `geography` como JSON legible en respuestas normales (llegan como WKB hexadecimal — confirmado revisando la guía oficial de PostGIS de Supabase), así que en vez de decodificar eso en JS se agregó `supabase/migrations/0003_matching_helpers.sql` con una función `distance_between_offers(uuid, uuid)` que calcula la distancia en metros directamente en Postgres (restringida a `service_role`, mismo patrón que `find_candidate_offers`). **Esta migración todavía no se ha aplicado al proyecto real** — pendiente que el usuario la corra (SQL Editor del panel de Supabase, o `supabase db push`).
  - `lib/supabase/admin.ts`: cliente con la llave de servicio, usado SOLO desde Server Actions (`lib/actions/consultar.ts`), nunca desde componentes de cliente. Cada función que lo usa valida a mano (contra `supabase.auth.getUser()` del cliente normal, no el admin) que el usuario autenticado es dueño de la oferta antes de dejarlo actuar — la llave de servicio salta RLS, así que esa validación manual es la única barrera.
  - La duración mostrada en `/consultar` es una estimación (distancia en línea recta ÷ velocidad promedio de 22 km/h) hasta que se conecte Google Distance Matrix en Fase 4 — está señalado en la UI, no se presenta como dato real.
  - `SUPABASE_SERVICE_ROLE_KEY` se agregó a `.env.local` y a las variables de entorno de Vercel (Production). **Nunca** lleva prefijo `NEXT_PUBLIC_`.
- 2026-08-17: Se armó la suite de tests end-to-end con **Playwright**, cubriendo el flujo completo de la demo: dos usuarios de prueba (`e2e.conductor@itam.mx` y `e2e.pasajero@itam.mx`) publican viajes compatibles en `/reserva`, se emparejan en `/consultar`, ambos confirman (pasajero primero, luego conductor) y el viaje confirmado aparece en `/manana` para los dos. Decisiones:
  - Los usuarios de prueba se crean/reutilizan automáticamente antes de cada corrida (`e2e/global-setup.ts`), usando la API de administración de Supabase Auth (`auth.admin.createUser` / `listUsers`) con `SUPABASE_SERVICE_ROLE_KEY` — así se saltan el flujo de correo real y no hace falta una bandeja de entrada. El mismo setup limpia `confirmed_trips`/`trip_offers`/`vehicles` de ambos usuarios antes de correr, para que la suite sea repetible.
  - `playwright.config.ts` puede correr contra `npm run dev` local (se levanta solo) o contra el deploy real de Vercel vía `PLAYWRIGHT_BASE_URL=https://<dominio> npm run test:e2e` — útil para probar la demo tal como la va a ver un inversionista, no solo en local.
  - Fix de UX que salió de escribir el test: `crearOferta` (`lib/actions/reserva.ts`) ahora regresa `success: true` al publicar, y `reserva-form.tsx` muestra un banner de confirmación ("¡Viaje publicado!") — antes el formulario no daba ninguna señal visual de éxito, lo cual además es la señal que usa el test para saber que el submit funcionó.
  - **Pendiente:** correr la suite por primera vez requiere `npm install` (para traer `@playwright/test`, ya en `package.json`) — no se pudo correr en este entorno de trabajo por no tener acceso al registro de npm, así que queda como siguiente paso del usuario. Instrucciones completas en la sección "Tests" de `README.md`.
- 2026-08-17: Al intentar `npx playwright install chromium` en la Mac del usuario (macOS 13/Ventura) falló con `Playwright does not support chromium on mac13` — desde hace tiempo Playwright dejó de publicar binarios de Chromium propios para macOS anterior a Sonoma (14). Se cambió `playwright.config.ts` para usar `channel: "chrome"` en vez del Chromium descargado por Playwright: los tests corren sobre el Google Chrome que ya está instalado en la máquina (necesario de todos modos para Claude en Chrome), así que no hace falta descargar ningún navegador ni depender de la versión de macOS. Confirmado por búsqueda en la documentación oficial de Playwright que `channel: "chrome"` evita el paso de `playwright install` por completo.
- 2026-08-17: Primera corrida real de `npm run test:e2e` falló en `global-setup.ts` con `Node.js detected but native WebSocket not found` al crear el cliente de Supabase con la llave de servicio. Causa: una versión reciente de `@supabase/supabase-js` (el rango `^2.45.0` del `package.json` resuelve a la última 2.x al correr `npm install`) exige `WebSocket` nativo del lado de Node, disponible solo desde Node 22 — el Node local del usuario es v20 (ver el warning de deprecación de Supabase en la misma corrida). En vez de pedirle que actualice Node a nivel de sistema (rompería otras herramientas y no protege a quien clone el repo con otra versión), se agregó la librería `ws` como dependencia y se le pasa explícitamente como `realtime.transport` en **ambos** lugares donde se crea un cliente con llave de servicio: `lib/supabase/admin.ts` (usado en producción por `/consultar`) y `e2e/global-setup.ts`. Con esto el proyecto funciona igual sin importar la versión de Node — se investigó y confirmó el fix exacto en la documentación pública de Supabase (`github.com/orgs/supabase/discussions/37869`). Como se agregó una dependencia nueva, hace falta correr `npm install` de nuevo antes de volver a probar.
- 2026-08-17: El test E2E encontró un bug real: al confirmar el viaje como conductor (segundo paso del emparejamiento), la base de datos sí se actualizaba pero la pantalla `/consultar` se quedaba con el botón "Elegir este viaje" visible, como si nada hubiera pasado. Causa: `revalidatePath("/consultar")` dentro de `elegirCandidato` invalida la caché del **servidor**, pero no obliga al **cliente** a re-pedir la ruta cuando la Server Action se llama fuera de un `<form action={...}>` (aquí se llama directo desde `onClick` dentro de `startTransition`, en `elegir-boton.tsx` y `cancelar-boton.tsx`). El paso del pasajero en el test "funcionaba" solo porque justo después hay una navegación completa (`page.goto`) que sí trae datos frescos — el bug estaba escondido porque ningún flujo anterior se había probado sin una navegación de por medio. Fix: se agregó `router.refresh()` (de `next/navigation`) después de una mutación exitosa en `elegir-boton.tsx` y, por el mismo patrón, también en `cancelar-boton.tsx` aunque el test no lo cubre directamente. Este bug ya afectaba producción (no solo el test) — cualquiera que confirmara un viaje como conductor sin recargar la página manualmente veía la pantalla desactualizada.
- 2026-08-17: El mismo test volvió a fallar exactamente igual después del fix de `router.refresh()`, sin ningún mensaje de error en pantalla — señal de que la falla real quedaba oculta en vez de resuelta. Se encontraron dos huecos: (1) `elegirCandidato`/`cancelarOferta` no tenían `try/catch`, así que cualquier excepción real (no un `.error` controlado de Supabase) rechazaba la promesa dentro del `startTransition` y React se lo tragaba en silencio, sin mostrar nada ni loguear nada; (2) `router.refresh()` se llamaba directo después de un `await` dentro del mismo `startTransition` async, que no es el patrón que documenta Next.js — la recomendación es envolverlo en su propio `startTransition` separado. Se corrigieron ambas cosas en `elegir-boton.tsx`/`cancelar-boton.tsx` (try/catch + `console.error` + refresh en su propio transition) y en `lib/actions/consultar.ts`/`lib/actions/cancelar.ts` (la lógica se movió a una función interna envuelta en try/catch en la exportada, logueando cualquier excepción con `console.error` — visible en la terminal de `npm run dev` y en el output `[WebServer]` que captura Playwright). Si el test vuelve a fallar, la terminal ahora debería mostrar el motivo real en vez de un fallo mudo.
- 2026-08-17: Tercera corrida — el test volvió a fallar en el mismo punto, esta vez sin ninguna excepción en la terminal. Se leyó `error-context.md` directo de la carpeta `test-results/` en la Mac del usuario (vía el puente de archivos) para ver el estado exacto en el momento de la falla: la duración estimada del candidato había cambiado entre corridas (~7 min → ~6 min), lo que confirmó que `router.refresh()` sí estaba funcionando y recalculando todo desde cero — pero seguía encontrando la misma oferta del conductor activa. **Bug real encontrado:** en `elegirCandidato`, el `delete` de `trip_offers` después de confirmar el viaje nunca revisaba su propio error. Si esas ofertas todavía estaban referenciadas por una fila de `trip_matches` (no se puede confirmar que el esquema real tenga `ON DELETE CASCADE` ahí, a pesar del comentario que lo asumía), el delete fallaba por violación de llave foránea — en silencio, porque el error nunca se leía — y la función igual regresaba `{success: true}` habiendo insertado `confirmed_trips` pero sin haber borrado las ofertas. Fix: ahora se borran a mano las filas de `trip_matches` que referencian esas ofertas *antes* de borrar las ofertas, y si el delete de ofertas falla de todos modos, se revisa el error y se regresa un mensaje en vez de tragárselo. Se aplicó la misma limpieza preventiva en `cancelarOferta` (ahora usa `lib/supabase/admin.ts` para ese paso) — un usuario cancelando una reservación que ya tenía un candidato calculado se hubiera topado con el mismo choque de llave foránea durante la demo real, aunque ahí sí se veía el error (confuso) en vez de fallar en silencio.
- 2026-08-17: Cuarta corrida — con el logging del fix anterior, el error real por fin apareció en la terminal: `violates foreign key constraint "confirmed_trips_match_id_fkey" on table "confirmed_trips"`. Diagnóstico definitivo revisando `supabase/migrations/0001_init_schema.sql`: `trip_offers -> trip_matches` sí tiene `on delete cascade` (el comentario original tenía razón en eso), pero `confirmed_trips.match_id -> trip_matches.id` **no** tiene cascade — a propósito, para conservar el vínculo histórico con el match que originó cada viaje confirmado. Eso significa que el `trip_matches` recién usado para crear un `confirmed_trips` es, por diseño del esquema, imposible de borrar (ni directo ni por cascada al borrar `trip_offers`) — el intento de "limpiar" agregado en la corrida anterior estaba resolviendo el síntoma equivocado. **Fix correcto:** en vez de borrar las ofertas, se actualiza su `status` a `'confirmado'` — valor que el enum `trip_offer_status` ya incluía (`buscando`/`confirmado`/`cancelado`/`expirado`) pero que nunca se había usado. `obtenerCandidatos()` ya filtra por `status = 'buscando'` en todos lados, así que una oferta confirmada deja de aparecer como candidato sin tocar `trip_matches` para nada. Se verificó que `/cancelar` (filtra por `status = 'buscando'`) y `/historial` (lee de `confirmed_trips`, no de `trip_offers`) no se ven afectados por dejar de borrar las filas de `trip_offers`.
- 2026-08-17: **Quinta corrida — la suite completa pasó de principio a fin.** El flujo que va a ver un inversionista (conductor y pasajero publican, se emparejan, ambos confirman, el viaje aparece en "mañana" para los dos) queda validado automáticamente contra el proyecto real de Supabase. En el camino se encontraron y corrigieron tres bugs reales que no se habían detectado a mano: (1) el paso 2 de la confirmación no refrescaba la pantalla sin recargar manualmente (`router.refresh()` faltante), (2) los errores de las Server Actions podían quedar completamente mudos si algo tronaba de forma inesperada, y (3) el `delete` de ofertas al confirmar un viaje violaba una llave foránea contra `confirmed_trips` y fallaba en silencio — el más serio de los tres, porque un viaje "confirmado" se hubiera quedado mostrándose como disponible para siempre. Este es el mejor argumento a favor de haber escrito la suite antes de la demo: los tres bugs eran invisibles probando a mano una sola vez, pero rompían el flujo real de un inversionista viendo el emparejamiento en detalle.
- 2026-08-17: El usuario aclaró el pitch real del producto (institucional, no exclusivo del ITAM; modelo de micro-earning conductor/pasajero) y eligió cerrar ambos gaps en el código antes de seguir con seed data o pulido visual — ver decisiones tomadas vía `AskUserQuestion`. Cambios:
  - **Multi-institución:** `supabase/migrations/0004_instituciones.sql` agrega la tabla `institutions` (nombre + dominio de correo), migra `profiles.institution_id` (`not null`, con el ITAM como valor de todos los perfiles existentes), reescribe `handle_new_user` para resolver la institución por dominio al crear el perfil, y reescribe `restrict_signup_to_itam_domain` para validar contra la tabla en vez de un dominio hardcodeado — **se mantiene el mismo nombre de función a propósito**, para no tener que reconectar el hook "Before User Created" en el panel de Supabase. `find_candidate_offers` ahora también exige `institution_id` igual entre ambas ofertas — sin esto, dos empresas cliente distintas se emparejarían entre sí. Se actualizó la copia de `/registro`, `/login` y `/home` para dejar de mencionar `@itam.mx` explícitamente. **Pendiente crítico: esta migración (`0004`) todavía no se ha aplicado al proyecto real de Supabase** — sin aplicarla, el registro seguiría funcionando con el Auth Hook viejo (que solo acepta `@itam.mx`) porque la función en la base de datos real todavía no se sobrescribió. Aplicar igual que las migraciones anteriores (SQL Editor o `supabase db push`), ver `docs/esquema_base_datos.md` sección 8.
  - **Precio/ganancia estimada:** `lib/pricing.ts` calcula un precio al pasajero y una ganancia al conductor a partir de la misma distancia en línea recta que ya se usa para la duración estimada — sin tabla ni columna nueva, sin cobro real todavía. Se muestra en `/consultar` ("Vas a pagar/ganar ~$X"), `/manana` y `/historial` (estos dos reconstruyen la distancia desde `trip_matches.estimated_duration_minutes`, embebido vía `confirmed_trips.match_id` — que sigue existiendo gracias al fix de la corrida anterior). Fórmula: tarifa base $10 MXN + $3.5 MXN/km, con 15% de comisión de plataforma sobre lo que paga el pasajero.
  - Se actualizó `docs/esquema_base_datos.md` (secciones 4, 5 y nueva sección 9) para reflejar ambos cambios y corregir la sección 5, que todavía describía el diseño viejo de "borrar" ofertas confirmadas.
- 2026-08-17: Seed de datos para la demo (`scripts/seed.ts`, `npm run seed`). El usuario pidió explícitamente enfocarlo en el **ITAM** — no en una segunda institución ficticia para probar multi-tenant — porque el ITAM mismo es un posible cliente/inversionista, así que la demo debe sentirse como una comunidad ITAM real. Decisiones:
  - 8 usuarios `@itam.mx` con nombres y direcciones plausibles en zonas reales de CDMX donde vive la comunidad del ITAM (Santa Fe, Interlomas, Del Valle, Coyoacán) — coordenadas reales aproximadas, suficientes para que el pre-filtro geoespacial (`ST_DWithin`) encuentre candidatos de verdad, no direcciones inventadas al azar.
  - 2 parejas **ya confirmadas** (inserta directo en `trip_matches`/`confirmed_trips` y marca las ofertas `status = 'confirmado'`, replicando a mano lo que hace `elegirCandidato`) para que `/manana` e `/historial` tengan contenido de inmediato al abrir la demo, sin depender de hacer clics en vivo.
  - 2 parejas se dejan **sin emparejar** (`status = 'buscando'`, sin fila en `trip_matches`) a propósito — para poder mostrar en vivo, frente al inversionista, cómo `/consultar` calcula candidatos y cómo se confirma un viaje paso a paso, en vez de que todo esté ya resuelto de antemano.
  - Los viajes son siempre "para mañana" (regla de negocio real de la app), así que el script calcula la fecha en el momento en que se corre — hay que correrlo el día antes de cada demo, no con anticipación. Documentado en `README.md` y en los comentarios del propio script.
  - Requiere que la migración `0004_instituciones.sql` ya esté aplicada (falla con mensaje claro si no encuentra la institución ITAM) y que `SUPABASE_SERVICE_ROLE_KEY` esté en `.env.local` (mismo patrón que `e2e/global-setup.ts`).
  - Se agregó `tsx` como devDependency (para correr el `.ts` directo con `npm run seed`, sin compilar) — requiere `npm install` antes de la primera corrida.
- 2026-08-18: Mientras se esperaba que el usuario corriera `npm install && npm run seed`, se hizo una revisión manual de `scripts/seed.ts` línea por línea contra el esquema real (`0001_init_schema.sql`, `0002_functions.sql`, `0003_matching_helpers.sql`, `0004_instituciones.sql`) para adelantar errores antes de la corrida real. No fue posible instalar dependencias en este entorno de trabajo para correr `tsc --noEmit` (el registro de npm respondió `403 Forbidden` para paquetes como `typescript`/`@supabase/supabase-js`/`@playwright/test`), así que la verificación fue de lectura/razonamiento contra el SQL real en vez de una corrida real. Se confirmó, columna por columna y constraint por constraint, que: (1) el insert a `trip_matches`/`confirmed_trips` en `confirmarPareja` cubre exactamente las columnas `not null` sin default de ambas tablas; (2) `crearOferta` respeta los 4 `check` de `trip_offers` (vehículo solo en conductor, `uses_toll_roads` siempre no-nulo en conductor, `meeting_point` obligatorio solo en conductor+regreso — coincide con las Parejas B y D, las únicas conductor+regreso del seed); (3) el orden de limpieza en `limpiarDatosPrevios` (`confirmed_trips` → `trip_offers`, dejando que el `on delete cascade` de `trip_offers → trip_matches` se encargue de esas filas) es justo el orden que evita el error de llave foránea que costó 4 corridas de E2E diagnosticar; (4) `handle_new_user` corre como trigger `after insert on auth.users`, así que también se dispara con `admin.auth.admin.createUser()` (no solo con el flujo de registro normal), por lo que el `profiles.institution_id` de los 8 usuarios del seed sí se resuelve solo; y (5) las distancias reales entre cada pareja C y D caben dentro del radio (`15km`) y la ventana de tiempo (`30min`) por defecto de `find_candidate_offers`, así que sí van a aparecer como candidatos mutuos en `/consultar` durante la demo en vivo. No se encontró ningún error. Sigue pendiente la corrida real del usuario como única verificación definitiva.
- 2026-08-18: Se confirmó por evidencia indirecta (archivos en la Mac del usuario: `package-lock.json` regenerado, `node_modules` presente, `test-results/.last-run.json` con `"status": "passed"`) que el usuario ya corrió `npm install` y la suite de E2E completa **después** de los cambios de multi-institución y precio/ganancia — sigue pasando de principio a fin. No hay evidencia todavía de que `npm run seed` se haya corrido (no se encontró forma de confirmarlo por archivos, a diferencia de los tests).
- 2026-08-18: Pulido visual (Fase 5) aplicado a las 8 pantallas. Como no se pudo instalar `next`/`typescript` en este entorno para compilar y probar en vivo, se trabajó con las mismas restricciones que en la revisión del seed: se trajeron todos los archivos reales de la Mac del usuario vía el puente de dispositivo (no solo los que se habían tocado en esta sesión), se revisó el bundle real de `lucide-react` ya instalado en su `node_modules` para confirmar que cada ícono usado existe en la versión exacta instalada (`0.441.0` — varios íconos comunes como `Home`, `PlusCircle`, `XCircle`, `CheckCircle2` fueron renombrados en versiones recientes de lucide, pero siguen exportados como alias de compatibilidad, confirmado línea por línea en el bundle), y se releyó `e2e/demo-flow.spec.ts`/`helpers.ts` para no romper ningún selector (`getByRole` por texto de botón, `getByText` con regex `/^Conductor/`/`/^Pasajero/`) — los íconos son SVG sin nodos de texto, así que no afectan ni el nombre accesible de los botones ni el `textContent` que usa Playwright. Cambios:
  - **Componente `Badge` nuevo** (`components/ui/badge.tsx`, mismo patrón `cva` que `Button`) con variantes extra `success`/`info` para poder codificar por color el status de un viaje (confirmado/programado en azul, precio/ganancia en verde) sin clases sueltas repetidas en cada pantalla.
  - **Nav con estado activo** (`components/app-nav.tsx`, client component nuevo porque necesita `usePathname`) con ícono por sección, reemplazando los links de texto plano de `app/(app)/layout.tsx`. El header ahora es sticky, tiene un wordmark con ícono, y muestra el nombre de la institución del usuario junto a "Carpool" (ya no dice "Carpool ITAM" fijo — coherente con el pitch institucional/multi-tenant).
  - **`/home` pasó de ser una sola tarjeta a un dashboard**: además de "Tu cuenta", ahora tiene una cuadrícula de accesos rápidos (ícono + descripción) a las otras 5 pantallas — es la primera pantalla que ve cualquiera después de iniciar sesión, así que era la que más se sentía como placeholder.
  - **`/cancelar`, `/consultar`, `/manana`, `/historial`**: el rol (conductor/pasajero), la dirección (ida/regreso) y el status ya no son texto plano separado por "·" — ahora son `Badge`s de color, con íconos junto a dirección/hora/punto de encuentro. El precio/ganancia (el diferenciador de negocio central, ver sección de arriba) se subió de texto verde discreto a un `Badge` verde con ícono de billetera, para que resalte más en la demo.
  - **`/reserva`**: los botones de rol (conductor/pasajero) y dirección (ida/regreso) ahora llevan ícono; el banner de éxito lleva un ícono de check. El texto de los botones (`"Conductor"`, `"Pasajero"`, `"Ida al ITAM"`, `"Regreso del ITAM"`, `"Publicar viaje"`) y del banner (`"¡Viaje publicado!"`) se dejó exactamente igual porque el E2E depende de ese texto exacto.
  - **`/login` y `/registro`**: el layout de auth (`app/(auth)/layout.tsx`) ahora tiene un wordmark simple arriba de la tarjeta; `/registro` agrega un ícono al estado "Revisa tu correo". El formulario y su lógica no cambiaron.
  - **Pendiente crítico:** no se pudo correr `npm run build` ni `npm run test:e2e` en este entorno de trabajo (mismo bloqueo de registro de npm que en el resto de la sesión) — la verificación fue leyendo cada archivo modificado contra el schema/E2E reales, no una corrida real. Antes de la demo, correr `npm run test:e2e` una vez más para confirmar que el pulido visual no rompió nada (no debería, por las razones de arriba, pero es la única verificación definitiva).
- 2026-08-18: El usuario corrió `npm run test:e2e` después del pulido visual y falló — pero **no por el pulido visual**. Diagnóstico completo leyendo `error-context.md` y las dos capturas de pantalla que deja Playwright en `test-results/` (misma técnica que las corridas de E2E de la sección anterior):
  - El test publica un conductor y un pasajero de ida a las 08:00 y usa `.getByRole("button", { name: "Elegir este viaje" }).first()` para que el pasajero elija un candidato, asumiendo que el único candidato posible es el otro usuario de prueba. Eso era cierto **antes de que existiera `scripts/seed.ts`**, pero ya no: con el seed corrido, `find_candidate_offers` también encuentra a los conductores/pasajeros de ejemplo del ITAM cuyo horario cae dentro de la ventana de 30 minutos del test (Valentina, conductora, 08:00 ida; Emilio, pasajero, 08:10 ida — ambos `status = 'buscando'`, los otros 6 usuarios del seed ya están `confirmado` o son de regreso y no aplican).
  - En la corrida que falló, el `.first()` del pasajero terminó eligiendo a **Valentina** (candidata del seed) en vez de al conductor de prueba, porque su duración estimada salió más corta. El conductor de prueba se quedó con dos candidatos sin confirmar (el pasajero de prueba y Emilio) y nunca aparece un botón para él — de ahí el timeout en el paso 2. Se confirmó comparando precio/duración exactos entre la vista del conductor y la del pasajero en las dos capturas: no coinciden entre sí, lo que prueba que cada uno terminó emparejado con una fila de `trip_matches` distinta.
  - **No es un bug del código de producción** (`lib/actions/consultar.ts` no cambió, y el comportamiento — mostrar todos los candidatos compatibles, incluidos los del seed — es exactamente el que se quiere en la demo real: demuestra que el emparejamiento escala a una comunidad completa, no solo a dos usuarios). Es una fragilidad del test: asumía que era el único tráfico en la base.
  - **Fix:** se cambió la hora publicada por `e2e/demo-flow.spec.ts` de `08:00` a `10:30` para ambos usuarios de prueba — más de 2 horas de distancia de cualquier horario de ida del seed (07:30/08:00/08:10), muy por fuera de la ventana de 30 minutos de `find_candidate_offers`, así que ya no hay forma de que un candidato del seed se cuele. No hizo falta tocar la dirección (el radio de 15 km ya no importa si la ventana de tiempo no coincide). Los datos huérfanos que dejó la corrida fallida (una fila de `trip_matches` entre el pasajero de prueba y Valentina, con `passenger_confirmed = true`) se limpian solos en la siguiente corrida — `e2e/global-setup.ts` borra las `trip_offers` de los usuarios de prueba al inicio de cada corrida, y eso borra en cascada (`on delete cascade`) cualquier `trip_matches` que las referencie; los datos del seed (Valentina, Emilio, etc.) no se tocan en absoluto.
  - **Nota operativa para la demo en vivo (no es un bug, es un aviso):** con el seed corrido, cualquier viaje que publiques tú mismo durante una demo en vivo con horario/zona parecida a los del seed va a mostrar varios candidatos a la vez (los del seed + los que publiques) — es una buena señal para el inversionista (el emparejamiento funciona con una comunidad real, no solo dos usuarios de juguete), pero si estás probando el flujo de confirmación tú mismo antes de la demo, fíjate en qué tarjeta le das clic — la app no muestra el nombre de la contraparte a propósito (por privacidad), solo rol/dirección/hora/precio.
- 2026-08-18: **La suite de E2E pasó de principio a fin** después de los dos fixes de arriba (hora aislada del seed, timeout de login más generoso) — confirmado por el usuario. El flujo completo (publicar → emparejar → confirmar ambos lados → aparecer en "mañana") queda validado otra vez contra el proyecto real, ya con seed de datos y multi-institución en juego.
- 2026-08-18: El usuario compartió el logo real de **WEPOOL** (mascota tipo rana en azul `#5170ff` + wordmark) y pidió usar esos colores como base del diseño. Se aplicó identidad de marca real en vez de placeholders genéricos:
  - **Assets**: el logo original (2000×2000, fondo blanco) se recortó y se le quitó el fondo (blanco → transparente) de forma programática. Se generaron: `public/logo-mascot.png` (solo la mascota, 512×512, para el header y usos pequeños), `public/logo-lockup.png` (mascota + wordmark "WEPOOL", para la pantalla de login/registro), `app/icon.png` y `app/apple-icon.png` (favicon — Next.js App Router los recoge solos con solo poner el archivo en `app/`, sin tocar `metadata`).
  - **Paleta** (`app/globals.css`): el azul del logo (`#5170ff` = `hsl(229 100% 66%)`) se guardó como `--brand` para usos decorativos, y como `--primary` se usa una versión un poco más oscura del mismo tono (`hsl(229 100% 60%)`) — con el azul del logo tal cual, el texto blanco de los botones queda en 4.1:1 de contraste (no llega al mínimo AA de 4.5:1 para texto normal); a 60% de luminosidad sube a 5.3:1. `--secondary`/`--accent`/`--border` se tiñeron levemente hacia el mismo azul (antes eran grises neutros de shadcn) para que hover states y badges secundarios se sientan parte de la misma marca en vez de genéricos. `--radius` subió de `0.5rem` a `0.75rem` (botones/inputs/badges un poco más redondeados, a tono con lo redondeado de la mascota) — los `Card` no cambian, ya usaban `rounded-xl` fijo de Tailwind.
  - **Reemplazo del ícono genérico**: el `Car` de `lucide-react` que se había usado como placeholder de logo (en el header de `(app)/layout.tsx` y `(auth)/layout.tsx`) se reemplazó por el logo real. El texto "Carpool" se cambió a "WEPOOL" en el header, el `<title>` (`app/layout.tsx`), `/registro`, `README.md` y `package.json` (`name: "wepool"`, ya coincide con el nombre del repo de GitHub). **No se tocó** nada específico del ITAM como institución (`restrict_signup_to_itam_domain`, "Ida al ITAM"/"Regreso del ITAM" en `/reserva`, los nombres de función en las migraciones) — eso es contenido específico de la institución piloto, no el nombre del producto, y parte de ese texto además lo verifica el E2E textualmente.
  - Se confirmó que ningún selector de `e2e/demo-flow.spec.ts`/`helpers.ts` depende del texto "Carpool" ni del ícono anterior — no había ningún `getByText`/`getByRole` apuntando al header o al wordmark, así que el cambio de marca no debería afectar la suite.
- 2026-08-18: Primera versión de **documentación de producto formal**, separada de las notas de desarrollo de este archivo — hasta ahora el pitch/modelo de negocio solo vivía como contexto disperso para decisiones de código (sección "Modelo de negocio / pitch" arriba). Dos documentos nuevos en `docs/`:
  - **`docs/producto.md`**: pitch completo (problema, solución, modelo de negocio con la fórmula de precio real de `lib/pricing.ts`, mercado/expansión institución por institución, diferenciadores frente a Uber/Didi y a coordinación informal, estado actual por pantalla, roadmap y métricas a trackear). Donde no había un dato real disponible (tamaño de mercado, comparación de precio con una cotización real de Uber/Didi, monto de ronda) se marcó explícitamente como **pendiente de definir** en vez de inventar una cifra — son huecos que hay que llenar con el fundador antes de usar el documento frente a un inversionista real.
  - **`docs/guion_demo.md`**: cierra el último pendiente de Fase 5. Narrativa pantalla por pantalla para la demo en vivo (con checklist operativo de antes-de-la-demo tomado de `README.md`: correr el seed un día antes, qué usuarios usar para que el emparejamiento del paso de `/consultar` sea real y no pre-armado), con las frases exactas de la UI citadas correctamente (`"¡Viaje publicado!"`, `"Elegir este viaje"`) para que el guion no se desincronice del producto real, y una tabla de preguntas frecuentes de inversionistas con respuestas honestas (incluye señalar explícitamente qué no está validado todavía, como el cobro real o la verificación de identidad del conductor).
- 2026-08-18: **WEPOOL ahora se puede instalar como app (PWA)** — el usuario preguntó si vale la pena una app nativa; se decidió (ver `docs/producto.md`, sección 12, para el razonamiento completo) que en esta etapa no se justifica un proyecto React Native/Expo aparte (código duplicado, cuentas de desarrollador, revisión de tienda) cuando una PWA da la mayor parte del beneficio percibido en horas, no semanas, sobre el mismo código. Se implementó con convenciones nativas de Next.js 15, sin ninguna librería nueva (`next-pwa` y similares no se pudieron instalar — mismo bloqueo de npm de siempre en este entorno de trabajo):
  - **`app/manifest.ts`**: nombre, iconos, `theme_color` (`#3358ff`, el mismo tono que `--primary` en `app/globals.css`), `start_url: "/"` (la raíz ya redirige a `/home` y de ahí el middleware rebota a `/login` si hace falta, así que no había que duplicar esa lógica). Next.js lo sirve solo en `/manifest.webmanifest` y agrega el `<link rel="manifest">` en el `<head>` sin tocar `app/layout.tsx` para eso.
  - **Iconos nuevos en `public/icons/`**: `icon-192.png`/`icon-512.png` (reusan el crop ya validado de `public/logo-mascot.png`, solo redimensionado) y `maskable-512.png` — este último tuvo que rehacerse una vez: el primer intento puso la mascota sobre fondo azul de marca y quedó casi invisible (la mascota es del mismo azul que el fondo, solo se veía el contorno por el antialiasing), así que se cambió a fondo blanco opaco, con la mascota escalada al 70% del lienzo para quedar dentro de la "zona segura" del 80% que exige el spec de iconos "maskable" (si no, algunos launchers de Android la recortan mal según la forma de ícono que use el usuario).
  - **`public/sw.js`** (service worker, sin librería): cachea únicamente el "app shell" estático (iconos, logo, manifest) para que abrir la app se sienta instantánea en visitas repetidas, con una página `public/offline.html` de respaldo si no hay red y tampoco hay nada en caché. A propósito **no cachea nada de Supabase** (sesión, viajes, resultados de `/consultar`) — cachear esas respuestas podría mostrar un viaje como disponible cuando ya se confirmó, por ejemplo.
  - **`components/register-sw.tsx`** (client component nuevo) registra el service worker desde `app/layout.tsx`; `app/layout.tsx` también gana `viewport.themeColor` (Next.js 14+ separó `themeColor` de `metadata` a `viewport` — ponerlo en el lugar viejo solo genera un warning y no se aplica) y `metadata.appleWebApp` (para que iOS trate la app instalada como app real: sin barra de Safari, con su propio título).
  - **`middleware.ts`**: se agregaron `sw.js` y `manifest.webmanifest` a las exclusiones del matcher — se piden en cada carga de la app, incluso sin sesión, así que no tenía sentido pasarlos por el cliente de Supabase en cada request.
  - **Pendiente crítico, igual que con el pulido visual y el rebrand**: no se pudo instalar la app en un dispositivo real ni correr `npm run build` en este entorno de trabajo — la verificación fue de lectura contra la documentación real de Next.js 15 (convención `app/manifest.ts`, separación `viewport`/`metadata`), no una prueba en vivo. Antes de la demo, instalarla una vez en un celular real (Android: menú de Chrome → "Agregar a la pantalla de inicio"; iOS: Safari → compartir → "Agregar a inicio", ahí no hay prompt automático como en Android) para confirmar que el ícono, el splash y la pantalla completa se ven bien.
- 2026-08-18: El usuario renombró la carpeta del proyecto de `mvp` a `wepool` (para que coincida con el nombre del repo de GitHub) y corrió `git push`, lo que disparó un build real en Vercel — **el primero de toda la sesión que sí corrió `tsc` de verdad**, y encontró un error de tipos que la verificación manual de este entorno de trabajo no podía detectar sin compilar:
  - **`app/(app)/home/page.tsx:59`**: `(profile?.institutions as { name: string } | null)?.name` — TypeScript rechazó el cast ("neither type sufficiently overlaps with the other"). El cliente de Supabase resuelve una relación embebida (`institutions(name)`) como objeto, arreglo de un objeto, o un tipo especial de error de su generador de tipos cuando no puede inferir bien la relación — ese tercer caso no comparte forma con `{ name: string }`, así que el cast directo no compila (a diferencia de cuando se revisó a mano contra el *esquema* de la base de datos, que sí era correcto — el error es puramente de los tipos que genera el cliente de Supabase, no del SQL).
  - Este mismo patrón estaba duplicado en `app/(app)/layout.tsx` (línea 34, idéntico) — el build solo alcanzó a reportar el primero, pero el segundo iba a fallar exactamente igual en la siguiente corrida.
  - **Fix:** en vez de parchar el cast en los dos archivos, se extrajo a `lib/institucion.ts` (`nombreInstitucion(institutions: unknown): string | undefined`) — normaliza los tres casos posibles con checks en tiempo de ejecución (`Array.isArray`, `typeof === "object"`, `"name" in value`) en vez de un cast de tipo, así que no hay forma de que vuelva a fallar la verificación de "overlap" del compilador sin importar qué tipo exacto genere la versión de `@supabase/ssr` instalada. `app/(app)/home/page.tsx` y `app/(app)/layout.tsx` ahora llaman a esa función en vez de repetir la lógica.
  - **Lección para el resto de esta sesión de trabajo:** todo lo demás (pulido visual, rebrand, PWA) se verificó leyendo código contra schema/E2E reales, pero **sin compilar** — es la primera vez que eso deja pasar un bug real. Sigue siendo la mejor verificación posible sin poder correr `npm install` aquí (el registro de npm sigue bloqueado), pero es una razón más para correr `npm run build` en local antes de cada `git push`, no solo confiar en que Vercel lo atrape.
- 2026-08-18: **Optimización de `/reserva` desde ambas perspectivas** (conductor y pasajero) — el usuario pidió pulir el formulario de publicar/reservar viaje; se identificaron cuatro fricciones concretas y se atacaron las cuatro:
  - **Selector de hora sin fecha.** `#scheduledTime` era un `<input type="datetime-local">` completo aunque la fecha SIEMPRE es "mañana" (regla de negocio ya validada server-side) — el usuario tenía que navegar un calendario para algo que no podía cambiar. Ahora es `<input type="time">`, con `07:30` como valor por default (la hora de ida más común en `scripts/seed.ts`). El servidor arma la fecha completa él mismo con `fechaDeMananaCDMX()` (`lib/actions/reserva.ts`) — esto además cierra un vector que ya no existe: antes, en teoría, un cliente podía mandar cualquier fecha en `scheduledTime` y dependía de un `.refine()` de zod para rechazarla; ahora el cliente solo puede mandar una hora, la fecha nunca sale de él.
  - **Toggle de "¿usas vías de cuota?"** — eran radios planos de HTML sueltos entre los toggles de botón (rol, dirección) que sí usan el estilo de la app. Se volvió un toggle Sí/No del mismo componente `Button` (tamaño `sm`, para diferenciarlo visualmente de los toggles principales de arriba). Se mantiene sin valor por default (fuerza una elección explícita, igual que el radio original) — pierde la validación nativa `required` del navegador, pero la validación de zod (`superRefine`) ya la cubre server-side igual que antes.
  - **Confirmación de dirección antes de publicar.** Antes el geocoding (Nominatim) solo corría en el servidor al dar clic en "Publicar viaje" — si la dirección no se entendía bien, el usuario se enteraba hasta después del submit. Ahora `ReservaForm` llama a un Server Action nuevo (`previsualizarDireccion`) al salir del campo de dirección (`onBlur`, no en cada tecla — respeta el límite de ~1 req/seg de Nominatim) y muestra la dirección normalizada que devolvió Nominatim (`display_name`) en una tarjeta verde de confirmación, o el error en rojo si no se pudo ubicar.
  - **Estimado de precio/ganancia en vivo.** Antes el precio/ganancia (`lib/pricing.ts`) solo se veía hasta que había un match real en `/consultar` — no reforzaba el pitch de negocio en el momento de decisión, en `/reserva` mismo. Ahora, si la institución del usuario tiene coordenadas de campus cargadas, `previsualizarDireccion` calcula la distancia en línea recta (Haversine, `lib/geocoding.ts::distanciaHaversineKm`) de la dirección escrita al campus y muestra un `Badge` verde ("Ganarías ~$X" para conductor, "Pagarías ~$X" para pasajero) junto a la confirmación de dirección, con la aclaración de que es un estimado. Requirió una migración nueva:
    - **`0005_campus_institucion.sql`**: agrega `campus_lat`/`campus_lng` (columnas planas `double precision`, no PostGIS — no hace falta índice espacial para esto, con Haversine en TypeScript basta) a `institutions`, y carga las coordenadas reales del campus Río Hondo del ITAM (verificadas por búsqueda web contra Google Maps, no se pudieron confirmar con una llamada directa a un geocoder desde este entorno de trabajo — mismo bloqueo de red de siempre para dominios fuera de la lista blanca del sandbox). **Pendiente aplicar esta migración en el proyecto real de Supabase**, igual que se hizo con `0004`.
    - Si una institución no tiene `campus_lat`/`campus_lng` cargados (`null`), el formulario simplemente no muestra el estimado — la confirmación de dirección sigue funcionando igual.
  - **Optimización de red, no solo UX:** para no duplicar la llamada a Nominatim (una para la confirmación en vivo, otra al publicar), `ReservaForm` manda las coordenadas ya verificadas como campos ocultos (`previewLat`, `previewLng`, `previewFor`) y `crearOferta` las reutiliza directamente si `previewFor` coincide exactamente con la dirección final — si el usuario editó la dirección después de verificarla (o si JS estaba deshabilitado, o la verificación falló), se cae automáticamente al geocoding normal de siempre. El camino común (usuario ve la confirmación, no vuelve a editar, publica) ahora hace **una sola** llamada de geocoding en vez de dos.
  - **`app/(app)/reserva/page.tsx`**: ahora también consulta `institutions.campus_lat/campus_lng` para pasárselas a `ReservaForm`. A propósito se hizo con dos `.select()` simples encadenados (perfil → institución) en vez de una relación embebida (`profiles.select("institutions(...)")")` — ese patrón fue exactamente lo que rompió el build hace un rato en `layout.tsx`/`home/page.tsx` (ver nota de arriba); dos queries directas no tienen ese riesgo de tipos.
  - **E2E actualizado** (`e2e/helpers.ts`, `publicarViaje`): `#scheduledTime` ahora se llena solo con la hora (ej. `"10:30"`, ya no `"2026-08-19T10:30"`), y el toggle de vías de cuota se hace clic por nombre accesible (`getByRole("button", { name: "Sí"|"No" })`) en vez de `.check()` sobre un radio que ya no existe. `demo-flow.spec.ts` no necesitó ningún cambio (ya pasaba `hora: "10:30"` como string simple, `helpers.ts` era el único que armaba el datetime completo). Se quitó `horaDeMananaParaInput`/la copia local de `fechaDeMananaCDMX` de `helpers.ts` por quedar sin uso.
  - **Pendiente, igual que el resto de esta sesión:** no se pudo correr `npm run build` ni el E2E real en este entorno de trabajo — verificado por lectura contra los archivos reales y contra la lección del bug anterior (evitar casts de tipos en selects con relaciones embebidas). Antes de la demo: (1) aplicar la migración `0005` en Supabase, (2) correr `npm run test:e2e` una vez para confirmar el flujo completo con los cambios de `#scheduledTime` y el toggle de vías de cuota, (3) probar el estimado de precio en vivo con una dirección real para confirmar que el número se ve razonable.
- 2026-08-18: El usuario reportó un hallazgo del **Security Advisor de Supabase**: *"Table `public.spatial_ref_sys` is public, but RLS has not been enabled"*. Investigado (búsqueda web, no es algo que se pueda diagnosticar desde el código del proyecto):
  - `spatial_ref_sys` **no es una tabla del proyecto** — la crea automáticamente la extensión PostGIS (habilitada en `public` desde `0001_init_schema.sql`) para guardar ~8000 definiciones de sistemas de referencia de coordenadas (SRID/EPSG) — es un catálogo de referencia público y de solo lectura, sin ningún dato de usuarios, viajes ni instituciones.
  - **No se puede habilitar RLS en esta tabla por los medios normales**: la crea y es dueña de ella el rol `supabase_admin`, no el rol del proyecto — `alter table` falla por permisos aunque se intente desde el SQL Editor. Es una limitación conocida y ampliamente reportada de Supabase + PostGIS cuando la extensión se instala en el esquema `public` (la práctica recomendada, instalar PostGIS en un esquema `extensions` aparte para que PostgREST no lo exponga en absoluto, solo aplica si se decide *antes* de instalar la extensión — cambiarlo ahora implicaría recrear la extensión, lo cual en teoría podría afectar la columna `home_location` de `trip_offers` que ya depende de ella; no es algo para hacer a la ligera con datos reales ya cargados).
  - **Recomendación:** intentar primero, en el SQL Editor de Supabase:
    ```sql
    revoke all on table public.spatial_ref_sys from anon, authenticated;
    ```
    Esto quita el acceso vía la API (que es la preocupación real de seguridad) sin depender de RLS. Si también falla por permisos (mismo dueño `supabase_admin`), el hallazgo se puede **descartar/ignorar** con confianza en el Security Advisor — no expone ningún dato sensible ni de la aplicación, es un patrón conocido y aceptado en proyectos Supabase con PostGIS en `public`. No se tocó ningún archivo del repo por este hallazgo — es configuración a nivel del proyecto de Supabase, no código.
  - Fuente: [discusión oficial de Supabase sobre este mismo hallazgo](https://github.com/orgs/supabase/discussions/26302).
- 2026-08-18/19: **Rediseño del home — feed de viajes tipo Rappi/BlaBlaCar.** El usuario pidió que el home dejara de ser un menú de accesos rápidos y se convirtiera en un feed en vivo, navegable por scroll, filtrado por una ubicación guardada (casa/oficina/otro) — reemplazando el flujo viejo de "publica tu propia oferta en /reserva y luego ve a revisar /consultar" por "elige directamente de lo que ya está publicado". Antes de construirlo se discutió con el usuario (a su pedido explícito, "quiero brainstormear") si debía haber cuentas separadas de conductor/pasajero al estilo Uber. Se recomendó **NO separar cuentas** — el carpool institucional depende de que la misma persona alterne de rol día a día según traiga o no el coche, así que meter fricción de registro tipo Uber (verificación, cuenta de "conductor" aparte) iría en contra de esa flexibilidad. En vez de eso, se separó solo la **navegación**: rol por viaje se queda igual (como ya funcionaba desde `/reserva`), pero el home ahora tiene dos modos de entrada claros — el feed (para buscar viaje) y un botón "Voy a manejar" (para publicar, sigue siendo `/reserva` sin tocar su lógica de fondo). El usuario confirmó este approach ("Si me hace sentido ese approach flexible") antes de implementarlo.
  - **Insight clave de diseño:** el feed no necesitó ningún dato nuevo de "oferta" — un conductor publicando su salida de mañana en `/reserva` (`role='conductor'`) YA ES exactamente la "oferta" que el feed necesita mostrar. Lo único que faltaba era (1) una forma de que el pasajero navegue esas ofertas sin tener que publicar la suya primero, y (2) una ubicación reusable desde donde buscar, en vez de escribir/geocodificar la dirección cada vez.
  - **`supabase/migrations/0006_saved_locations.sql`** (NUEVA, pendiente de aplicar en Supabase — igual que 0004/0005): tabla `saved_locations` (hasta 3 filas por usuario, `unique(user_id, kind)` con `kind in ('casa','oficina','otro')` — set fijo, no soporta ubicaciones arbitrarias por decisión de producto). Guarda `lat`/`lng` en columnas planas ADEMÁS de `location geography(Point,4326)` — a diferencia de `trip_offers`, aquí sí hace falta leer las coordenadas de vuelta desde código de servidor normal (PostgREST no decodifica columnas `geography`, ver nota de `0003_matching_helpers.sql`). RLS igual que `vehicles` (el dueño administra las suyas directo, sin pasar por el cliente admin). También agrega la función `find_driver_offers_near(p_lat, p_lng, p_institution_id, p_start_time, p_end_time, p_radius_km, p_limit)`, análoga a `find_candidate_offers` pero partiendo de un punto arbitrario en vez de una `trip_offer` propia — restringida a `service_role` por el mismo motivo. **Decisión de producto explícita:** esta función SÍ expone el nombre (primer nombre solamente, recortado en `lib/actions/feed.ts`) y la descripción del vehículo del conductor antes de que exista un match confirmado — más de lo que el flujo viejo de `/consultar` expone (que no muestra nombre ni vehículo hasta `confirmed_trips`). Es una concesión deliberada para que el feed se sienta como el de una plataforma real (BlaBlaCar/Uber muestran esto antes de reservar) — NUNCA expone `home_address`/`home_location` exactos, solo `distance_meters` ya calculada en el servidor, igual que el resto de la app.
  - **`lib/actions/ubicaciones.ts`** (NUEVO): `listarUbicaciones()` (lee las ubicaciones guardadas del usuario) y `guardarUbicacion()` (Server Action tipo `useActionState`, geocodifica con `geocodificarDireccion` ya existente y hace upsert sobre `(user_id, kind)` — "cambiar dirección" es simplemente volver a guardar el mismo `kind`).
  - **`lib/actions/feed.ts`** (NUEVO): `obtenerFeed(savedLocationId)` llama a `find_driver_offers_near` (vía cliente admin) filtrando por la ventana de "mañana" (`rangoUTCDeManana`, ya existente) y calcula precio estimado con `estimarPrecioViaje` (ya existente) desde `distance_meters`. `unirmeAViaje(driverOfferId, savedLocationId)` colapsa en un solo paso lo que antes eran dos pasos separados (publicar oferta propia + confirmarla en `/consultar`): valida que la oferta del conductor siga `'buscando'`, crea la `trip_offer` del pasajero usando la ubicación guardada, calcula la duración real vía `distance_between_offers` (ya existente) e inserta el `trip_match` con `passenger_confirmed = true` directo — el conductor sigue confirmando desde `/consultar` exactamente como antes (`elegirCandidato`, sin ningún cambio). No se tocó nada de `lib/actions/consultar.ts`: ambos caminos (feed nuevo, `/reserva`+`/consultar` viejo) siguen interoperando porque al final solo crean filas normales en `trip_offers`/`trip_matches`.
  - **Componentes nuevos**: `components/location-switcher.tsx` (Server Component — son solo `<Link>`s con querystring `?loc=casa|oficina|otro`, no necesita estado de cliente), `components/ubicacion-form.tsx` (formulario para agregar/cambiar la dirección de un `kind`, mismo patrón `useActionState` que el resto de la app), `components/unirme-boton.tsx` (mismo patrón que `components/elegir-boton.tsx` existente: `useTransition` + manejo de excepción + `router.refresh()`).
  - **`app/(app)/home/page.tsx`** (REESCRITO): ya no es una cuadrícula de tarjetas de navegación — ahora lee `searchParams.loc` (Next.js 15, `searchParams` es una Promise) para saber qué ubicación mostrar, renderiza el switcher de ubicación, y si la ubicación seleccionada ya tiene dirección guardada, el feed de tarjetas (conductor, ida/regreso, hora, distancia, precio estimado, vehículo, botón "Unirme a este viaje"); si no, un formulario inline para guardarla la primera vez. Si el feed está vacío se explica que los conductores publican la noche anterior, con un link a `/reserva`. Los accesos que ya no son el flujo principal (Consultar del lado conductor, Mañana, Historial, Cancelar) se movieron a una fila compacta de botones al final en vez de tarjetas grandes — siguen accesibles ahí y en `AppNav` (que no cambió).
  - **`components/reserva-form.tsx`**: único cambio, el toggle de rol ahora arranca en `"conductor"` en vez de `"pasajero"` — el único camino de entrada normal a `/reserva` ahora es el botón "Voy a manejar" del home. El formulario en sí (incluyendo la opción de publicarse como pasajero manualmente) no se tocó.
  - **Alcance deliberadamente NO incluido en este cambio** (para no romper nada que ya funciona y mantener el diff enfocado): no se quitó la opción de rol "pasajero" de `/reserva` (queda como respaldo manual); `/consultar` no cambió (el conductor lo sigue usando para confirmar candidatos); no se tocó `e2e/helpers.ts`/`demo-flow.spec.ts` — el flujo que prueban (publicar como conductor y pasajero vía `/reserva`, confirmar vía `/consultar`) sigue existiendo intacto y sin cambios, así que la suite no debería romperse, pero **no cubre el feed nuevo** — sigue pendiente escribir un test E2E para el camino "buscar en el feed → unirme → el conductor confirma en /consultar", ver pendientes abajo.
  - **Pendiente crítico, igual que el resto de esta sesión:** no se pudo correr `npm run build` ni el E2E en este entorno de trabajo (mismo bloqueo de npm de siempre) — verificado por lectura contra el schema real y los tipos de cada Server Action, no una corrida real. Antes de la demo: (1) **aplicar la migración `0006` en Supabase** (junto con las `0004`/`0005` pendientes, si no se habían aplicado ya), (2) correr `npm run build` y `npm run test:e2e` para confirmar que nada se rompió, (3) probar el flujo del feed con dos usuarios reales (uno publica en `/reserva` como conductor, el otro lo ve y se une desde el home) para confirmar que las tarjetas y el precio estimado se ven razonables, (4) escribir un test E2E nuevo para el camino del feed (hoy solo está cubierto el camino viejo `/reserva`+`/consultar` en ambos roles).
- 2026-08-19: **Candado temporal de acceso.** El usuario compró el dominio real y le preocupó quedar expuesto mientras el producto sigue en desarrollo. Antes de implementar nada se investigó qué tan expuesto está en realidad: cada tabla de Supabase ya tiene RLS habilitada (cada quien solo lee/escribe lo suyo, ver `0001_init_schema.sql`) y el registro ya está restringido por dominio institucional (`restrict_signup_to_itam_domain`, `0004_instituciones.sql`) — así que un desconocido que encuentre el dominio no puede crear cuenta ni leer datos de nadie; lo único expuesto de verdad son las pantallas públicas (`/login`, `/registro`) y la posibilidad de que Google las indexe. Se le presentaron dos caminos: la protección nativa de Vercel (más pulida, pero bloquear el dominio de *producción* — no solo previews — requiere plan Pro, ~$20/mes, y una contraseña compartida sin cuenta de Vercel para gente externa como un inversionista requiere un add-on de +$150/mes encima de Pro) contra un candado a nivel de código, gratis en cualquier plan. Se optó por la opción gratis. Implementado:
  - **`middleware.ts`**: se agregó `candadoDesarrollo()`, que corre antes que `updateSession` (la lógica de sesión de Supabase que ya existía) y responde `401` con el header `WWW-Authenticate: Basic` si no vienen credenciales HTTP Basic Auth válidas — el navegador muestra su propio prompt nativo de usuario/contraseña, no hace falta ninguna pantalla ni librería nueva. Decodifica las credenciales con `atob` (no con `Buffer`, que no está garantizado en el Edge Runtime donde corre el middleware — mismo tipo de precaución que ya costó un build real roto antes en esta sesión, ver la nota de `home/page.tsx`/`layout.tsx` más arriba: no asumir disponibilidad de una API sin verificarla contra el runtime real).
  - **Se activa solo si `SITE_LOCK_PASSWORD` está definida** como variable de entorno (`SITE_LOCK_USER` es opcional, por default `"wepool"`) — mientras no se configure en Vercel (o en `.env.local`), el sitio se comporta exactamente igual que hoy, sin candado. Eso significa que el candado se prende/apaga sin tocar código: se agrega o se borra la variable en Vercel → Settings → Environment Variables y se vuelve a desplegar. `env-example.txt` documenta las dos variables nuevas.
  - **No cubre absolutamente todo a propósito**: el `matcher` de `middleware.ts` ya excluía `_next/static`, `_next/image`, `favicon.ico`, `sw.js`, `manifest.webmanifest` y extensiones de imagen (por rendimiento y porque el service worker/manifest de la PWA se piden sin sesión) — esos siguen sin pasar por el candado. No es un problema real: lo único que queda visible sin contraseña son assets estáticos sin ningún dato (el logo, los íconos, el manifest de la PWA), nunca una pantalla de la app ni una respuesta con datos.
  - **`app/robots.ts`** (nuevo, mismo patrón que `app/manifest.ts`) y **`metadata.robots` en `app/layout.tsx`** (`index: false, follow: false`): piden a los buscadores no indexar ni seguir enlaces del sitio mientras tanto — un candado de contraseña no evita que Google encuentre y liste la URL igual, solo que alguien pueda *entrar*. Ninguno de los dos sustituye el candado (alguien con el link directo lo puede seguir intentando abrir, solo que ahora le pide contraseña) — son complementarios, no redundantes.
  - **Cuándo quitarlo:** cuando el producto esté listo para mostrarse públicamente, basta con (1) borrar `SITE_LOCK_PASSWORD` de las variables de entorno de Vercel y volver a desplegar, y (2) revertir `metadata.robots` en `app/layout.tsx` y el `disallow` de `app/robots.ts` (o simplemente borrar ambos archivos/campos) para volver a permitir indexación.
  - **Pendiente:** el usuario todavía tiene que (1) elegir una contraseña real y configurar `SITE_LOCK_USER`/`SITE_LOCK_PASSWORD` en Vercel → Settings → Environment Variables (Production **y** Preview, si quiere que ambos queden cerrados), (2) volver a desplegar para que tome efecto, y (3) confirmar entrando al dominio real que el navegador pide la contraseña antes de mostrar nada. No se pudo probar el prompt de Basic Auth en vivo en este entorno de trabajo (sin navegador con acceso al dominio real desplegado).
- 2026-08-19: **Integración de Google Maps — tiempo de viaje real.** El usuario pidió conectar la API de Google Maps para calcular el tiempo de los viajes ofrecidos, reemplazando el estimado de línea recta (PostGIS `ST_Distance`) + velocidad promedio constante (`VELOCIDAD_PROMEDIO_KMH = 22`) que se usaba desde el principio del proyecto — este estimado siempre fue un placeholder documentado como tal ("Fase 4: Google Distance Matrix", ver comentarios originales de `lib/pricing.ts`/`lib/actions/consultar.ts`).
  - **Investigación previa (API actual de Google, no se asumió nada del entrenamiento):** se confirmó que la **Distance Matrix API clásica** (`distancematrix.googleapis.com`) sigue funcionando pero Google la posiciona como reemplazada desde 2024 — toda documentación nueva apunta a la **Routes API** (`routes.googleapis.com`, endpoint `computeRouteMatrix`), así que se construyó sobre esa. Precio: nivel **Essentials** (sin tráfico en tiempo real) $5/1000 elementos con 10,000 gratis al mes; nivel **Pro** (con `routingPreference: TRAFFIC_AWARE`) $10/1000 con 5,000 gratis al mes. **Decisión de producto:** se implementó en nivel Essentials (sin tráfico en tiempo real) — para un piloto en etapa MVP la diferencia de precisión no justifica duplicar el costo y reducir a la mitad el nivel gratuito; subir a tráfico real después es agregar un solo campo en `lib/rutas.ts`. Requiere cuenta de facturación en Google Cloud (pide tarjeta aunque no se rebase el nivel gratuito) y habilitar específicamente "Routes API" — no basta con tener una llave vieja de otra API de Maps.
  - **Alcance deliberado:** solo se conectó Google para **tiempo/distancia de manejo** (lo que se pidió). El **geocoding** (dirección de texto → coordenadas) se queda en Nominatim/OpenStreetMap (`lib/geocoding.ts`, sin cambios) — funciona bien, es gratis, y no había ninguna razón de negocio para pagar por Google Geocoding además de Routes API solo por consistencia. Si más adelante se quiere mover también el geocoding a Google, es un cambio aislado a ese archivo (ya lo anticipa su comentario original).
  - **`lib/rutas.ts`** (nuevo): `calcularMatrizRutas(origen, destinos[])` — UNA llamada a `computeRouteMatrix` por origen (no una por candidato), aprovechando que el pre-filtro geoespacial de PostGIS (`find_candidate_offers`/`find_driver_offers_near`, ya existentes) ya reduce la lista a ~20-30 candidatos antes de llegar aquí; exactamente el diseño que los comentarios de `0002_functions.sql` ya anticipaban desde el principio del proyecto ("solo los ~N candidatos más cercanos se mandan después a la API de Google... para confirmar el desvío real"). También `calcularRutaReal(origen, destino)` para el caso de un solo par. **Nunca lanza una excepción** — si `GOOGLE_MAPS_API_KEY` no está configurada, la llamada falla, o Google no encuentra ruta para un par específico, regresa `null` en esa posición en vez de tronar todo; quien llama decide el fallback. Esto es deliberado: mientras el usuario no termine de configurar la llave (crear el proyecto de Google Cloud, habilitar Routes API, vincular facturación — sigue pendiente, ver abajo), la app debe seguir funcionando exactamente igual que hoy, solo sin la mejora de precisión.
  - **`supabase/migrations/0007_rutas_reales.sql`** (NUEVA, pendiente de aplicar en Supabase — junto con la `0006` que tampoco se había confirmado aplicada): (1) `trip_offers` gana columnas planas `home_lat`/`home_lng` (con backfill de las filas existentes vía `ST_Y`/`ST_X` sobre la columna `geography` que ya tenían) — mismo motivo que `saved_locations` en `0006`: PostgREST no decodifica columnas `geography`, así que hace falta poder leer coordenadas de vuelta desde código de servidor normal para pasárselas a Google. (2) `trip_matches` gana `distance_km` (nullable) — la distancia REAL de manejo que devuelve Google, para que el precio (que se calcula por distancia, `estimarPrecioViaje`) no tenga que reconstruirse invirtiendo la duración con la velocidad promedio constante; eso dejó de ser seguro en cuanto la duración guardada empezó a poder ser real y no derivada de esa misma constante — habría sido una inconsistencia nueva y silenciosa si no se corregía. (3) `find_driver_offers_near` (de `0006`) se actualiza con `create or replace` para regresar también `home_lat`/`home_lng` de cada conductor candidato — `find_candidate_offers` no necesitó ningún cambio porque ya regresa `setof trip_offers` completo (`select o2.*`), así que hereda las columnas nuevas solo.
  - **`lib/actions/consultar.ts::obtenerCandidatos`**: por cada oferta propia, ahora hace UNA llamada a `calcularMatrizRutas` con todos los candidatos ya prefiltrados (antes: una llamada a `distance_between_offers` por candidato, siempre). Si Google no pudo calcular la ruta de un candidato específico, ese candidato individual cae al fallback de siempre (`distance_between_offers` + la nueva `duracionDesdeMetros` en `lib/pricing.ts`, que centraliza una cuenta que antes estaba repetida) — así un problema con Google nunca tira toda la lista de candidatos, solo hace que algunos vuelvan a ser estimados en vez de reales. El precio ya no se deriva invirtiendo la duración; usa `trip_matches.distance_km` directo cuando existe.
  - **`lib/actions/feed.ts`**: `obtenerFeed` hace lo mismo (una llamada por carga del feed, no por tarjeta) y ahora expone `duracionMinutos` en `FeedCandidato` — el feed del home **no mostraba ningún tiempo de trayecto hasta ahora**, solo distancia y precio, así que esto también es contenido nuevo en pantalla, no solo mayor precisión. `unirmeAViaje` usa `calcularRutaReal` (un solo par, ya se sabe con qué conductor específico se emparejó) para guardar duración y distancia reales en el `trip_match` que crea.
  - **`app/(app)/home/page.tsx`**: cada tarjeta del feed ahora muestra "~X min de trayecto" junto a la distancia, cuando Google pudo calcularla (si no, se sigue mostrando solo distancia, como antes).
  - **`lib/pricing.ts`**: `TripMatchEmbebido` ahora incluye `distance_km`; nueva `precioDeMatchEmbebido()` que usa la distancia real cuando existe y cae a `estimarPrecioDesdeDuracionMinutos` (sin cambios, se queda como fallback) cuando no. `/manana` y `/historial` (`app/(app)/manana/page.tsx`, `app/(app)/historial/page.tsx`) se actualizaron para pedir `distance_km` en su select embebido de `trip_matches` y usar esta función nueva en vez de encadenar `duracionDeMatchEmbebido` + `estimarPrecioDesdeDuracionMinutos` a mano — sin cambio visual, solo la fuente del número.
  - **`lib/actions/reserva.ts`**: `crearOferta` ahora también guarda `home_lat`/`home_lng` planos al publicar un viaje (antes solo `home_location`) — necesario para que ese viaje pueda usarse como origen/destino en `calcularMatrizRutas` sin decodificar geography.
  - **No se tocó** `previsualizarDireccion` (el estimado de precio en vivo en `/reserva`, contra el campus fijo de la institución) — se sigue calculando con Haversine (`distanciaHaversineKm`), no con Google. Es un estimado *antes* de publicar contra un punto fijo, no un viaje real emparejado con otra persona; conectarlo a Google también es una mejora razonable a futuro, pero no era lo que se pidió y hubiera aumentado el volumen de llamadas (cada tecleo/blur del formulario) sin necesidad real.
  - **Pendiente crítico:** (1) el usuario tiene que crear el proyecto de Google Cloud, habilitar "Routes API" específicamente, vincular una cuenta de facturación, generar la llave (restringida a "Routes API", sin restricción de IP — Vercel no da IP fija en el plan gratuito) y ponerla en `.env.local`/Vercel como `GOOGLE_MAPS_API_KEY` (ver `env-example.txt`, ya documentado) — sin esto, la app sigue funcionando exactamente igual que ayer (fallback de línea recta), no hay urgencia de bloquear nada por esto. (2) aplicar `0007_rutas_reales.sql` en Supabase (y confirmar que `0006` también ya se aplicó, seguía pendiente). (3) no se pudo correr `npm run build` ni probar una llamada real a Google en este entorno de trabajo (mismo bloqueo de npm/dominios de siempre) — verificado por lectura contra la documentación real de Routes API (`computeRouteMatrix`, field mask, forma de respuesta) citada arriba, no una corrida en vivo. Antes de la demo: probar con la llave real configurada que el tiempo mostrado en el feed y en `/consultar` se vea razonable (no solo que no truene).
